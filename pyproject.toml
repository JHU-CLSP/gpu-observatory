[project]
name = "gpu-stats-ia1-lab"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.9"  # 3.9+ supported via `from __future__ import annotations`; 3.10+ works natively
dependencies = [
    "fastapi>=0.110",
    "uvicorn>=0.29",
    "aiofiles>=23.0",
]
